VITE_API_BASE_URL=http://localhost:8000
# Show internal-only tools (Evaluation page, prompt-preview section).
# Set to "true" in dev builds; leave unset / "false" in prod.
VITE_SHOW_INTERNAL_TOOLS=true
